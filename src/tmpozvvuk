pub struct Invader {
	x: usize,
	y: usize,
}

pub struct Invaders {
	pub army: Vec<Invader>,
	move_timer: Timer,
	direction: i32,
}

impl Invders {
	pub fn new() -> Self {
		let mut army = Vec::new();
		for x in 0..NUM_COLS {
			for y un 0..NUM_ROWS {

			}
		}
		Self {
			army, 
			move_timer: Timer::from_m,
		}
	}
}