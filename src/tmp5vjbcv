use rusty_audio::Audio;
use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    let mut audio = Audio::new();
    audio.add("explode", "expl.wav");
    audio.add("", ".wav");
    audio.add("", ".wav");
    audio.add("", ".wav");
    audio.add("", ".wav");
    Ok(())
}
